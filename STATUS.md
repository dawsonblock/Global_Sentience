# Status

- Current authoritative runtime: Python under `src/global_workspace_runtime/`.
- Rust workspace status: kernel-in-progress under `global-workspace-runtime-rs/`.
- Legacy Rust status: deprecated under `runtime/kernel/`.
- LLM status: mock-only by default; non-mock adapter modes are not implemented.
- Archive status: JSONL backend is active.
- Memvid status: vendored/stubbed, not active in runtime.
- SimWorld status: deterministic regression harness, not proof of sentience or
  subjective experience.
- Safety status: research scaffold only, not a real-world safety guarantee.
- Privacy note: generated traces and memory logs may contain user input; commit
  only sanitized fixtures.